class CartItem {
  constructor(title, price, quantity, sum) {
    (this.title = title),
      (this.price = price),
      (this.quantity = quantity),
      (this.sum = sum);
  }
}

export default CartItem;
